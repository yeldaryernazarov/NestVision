# Telegram Channel Forwarder

Автоматический слушатель и пересылка сообщений из Telegram канала в бота.

## 🎯 Назначение

Этот скрипт **постоянно работает** и автоматически пересылает все новые сообщения из канала `nest-pre` в ваш личный чат, откуда вы можете переслать их боту `@NestVesionVideoDownloaderBot` для обработки.

## 📋 Требования

- Node.js >= 18
- npm или yarn
- Бот должен быть администратором канала с правом "Читать сообщения"

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd telegram-forwarder
npm install
```

### 2. Узнайте ваш User ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш `Id` (например, `1956288165`)

### 3. Настройте скрипт

Откройте `forwarder-simple.js` и укажите ваш User ID:

```javascript
const YOUR_USER_ID = '1956288165'; // ваш ID
```

Или используйте переменную окружения:

```bash
USER_ID=1956288165 node forwarder-simple.js
```

### 4. Добавьте бота в канал

1. Откройте канал `nest-pre`
2. Перейдите в "Управление каналом" → "Администраторы"
3. Добавьте бота `@NestVesionVideoDownloaderBot`
4. **Включите право "Читать сообщения"**

### 5. Запустите скрипт

```bash
node forwarder-simple.js
```

Или используйте скрипт запуска:

```bash
./start.sh
```

## 🔄 Как это работает

1. **Скрипт запускается** и подключается к Telegram Bot API
2. **Слушает канал** постоянно (работает как фоновый процесс)
3. **При получении нового сообщения** из канала:
   - Логирует информацию о сообщении
   - **Автоматически пересылает** сообщение в ваш личный чат
4. **Вы пересылаете** сообщение боту `@NestVesionVideoDownloaderBot`
5. **Java backend** автоматически обрабатывает пересланное сообщение

## 📝 Логирование

Скрипт выводит в консоль:
- 📨 Полученные сообщения из канала
- ✅ Успешные пересылки
- ❌ Ошибки

## 🔧 Запуск в фоновом режиме

### Вариант 1: Используя nohup

```bash
nohup node forwarder-simple.js > forwarder.log 2>&1 &
```

### Вариант 2: Используя screen

```bash
screen -S forwarder
node forwarder-simple.js
# Нажмите Ctrl+A, затем D для отсоединения
```

### Вариант 3: Используя pm2 (рекомендуется)

```bash
npm install -g pm2
pm2 start forwarder-simple.js --name telegram-forwarder
pm2 save
pm2 startup
```

## 🛑 Остановка

Нажмите `Ctrl+C` для graceful shutdown.

Если запущен в фоне:
```bash
# Для pm2
pm2 stop telegram-forwarder

# Для nohup
pkill -f forwarder-simple.js
```

## ⚠️ Важно

- **Скрипт должен работать постоянно** для автоматической пересылки
- Бот должен быть администратором канала с правом "Читать сообщения"
- Сообщения пересылаются в ваш личный чат - оттуда пересылайте их боту
- Для полностью автоматической обработки можно настроить автоматическую пересылку из личного чата в бота (через настройки Telegram или дополнительный скрипт)

## 🔄 Полностью автоматическая система

Для полностью автоматической обработки (без ручной пересылки):

1. Скрипт пересылает сообщения в ваш личный чат
2. Настройте автоматическую пересылку из личного чата в бота через:
   - Telegram настройки (если доступно)
   - Дополнительный скрипт, который слушает ваш личный чат и пересылает боту

## 📊 Мониторинг

Проверьте, что скрипт работает:

```bash
# Проверьте логи
tail -f forwarder.log

# Или если используете pm2
pm2 logs telegram-forwarder
```

## 🐛 Решение проблем

### Скрипт не получает сообщения

1. Проверьте, что бот добавлен в канал как администратор
2. Убедитесь, что включено право "Читать сообщения"
3. Проверьте токен бота

### Ошибка "chat not found"

- Проверьте, что USER_ID указан правильно
- Узнайте ваш ID у @userinfobot

### Скрипт останавливается

- Используйте pm2 или nohup для фонового запуска
- Проверьте логи на наличие ошибок
